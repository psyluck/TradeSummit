pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Running a build script...'
                // You will add your actual build commands here later
            }
        }
    }
}               //just a sample comment to see some changes and test it